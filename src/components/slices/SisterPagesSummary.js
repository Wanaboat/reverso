import React from 'react'
import {
    Box
} from '@chakra-ui/core'

const SisterPagesSummary = ( props ) => {
    return(
        <Box>
            SisterPagesSummary
        </Box>
    )
}

export default SisterPagesSummary